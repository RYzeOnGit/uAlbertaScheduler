// Using express, Zod etc. to code a pretty cool backend
const express = require('express');
const { z } = require('zod');

const app = express(); // Creating an express app

console.log('Hello, world!');


app.listen(5173); // Aligning with the port in the frontend